import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Ayuda y FAQ — COPOSA' }

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
