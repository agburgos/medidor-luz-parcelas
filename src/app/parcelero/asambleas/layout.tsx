import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Asambleas y Actas — COPOSA' }

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
