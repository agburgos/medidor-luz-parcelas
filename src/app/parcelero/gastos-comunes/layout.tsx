import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Gastos Comunes — COPOSA' }

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
