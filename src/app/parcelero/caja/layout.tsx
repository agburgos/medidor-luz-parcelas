import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Caja y Tesorería — COPOSA' }

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
