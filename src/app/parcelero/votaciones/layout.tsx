import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Votaciones — COPOSA' }

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
